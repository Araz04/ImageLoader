package com.ideascollection.imageloader.core

object ImageLoader {
    fun load(
        url: String,
        placeholder: Int,
        imageView: ImageView
    ) {
        // later implementation
    }

    fun clearCache() {
        // later
    }
}